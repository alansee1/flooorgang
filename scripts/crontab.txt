# FlooorGang Automated Scheduler
# Add to crontab on Raspberry Pi with: crontab scripts/crontab.txt

# Set shell and path
SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

# Daily scheduler - Runs at 8 AM PST
# Checks for today's games and schedules scanner 3 hours before first game
0 8 * * * cd ~/Projects/flooorgang && venv/bin/python3 scripts/schedule_scanner.py >> logs/scheduler.log 2>&1

# Results tracker - Runs at 9 AM PST next day
# Scores yesterday's picks with NBA stats (unscored only)
0 9 * * * cd ~/Projects/flooorgang && bash scripts/run_results_tracker.sh >> logs/results.log 2>&1
